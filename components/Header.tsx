import { Text, View } from "react-native";

const Header = () => {
  return (
    <View className="flex justify-center">
      <Text className="text-center py-2 bg-blue-300">Header</Text>
    </View>
  );
}

export default Header;